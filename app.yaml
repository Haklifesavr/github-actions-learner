runtime: nodejs16 # or another supported version
service: github-actions-learner
handlers:

- url: /(.*\..+)$
  static_files: build/\1
  upload: build/(.*\..+)$

- url: /.*
  static_files: build/index.html
  upload: build/index.html